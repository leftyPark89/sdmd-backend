<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sdmd.sdmdbackend.mapper.UserMapper">

    <select id="findByEmail" resultType="com.sdmd.sdmdbackend.entity.User">
        SELECT id,
               nickname,
               email,
               password
        FROM user
        WHERE email = #{email}
    </select>


    <insert id="insertUser">
        INSERT INTO user
            (nickname, email, password, login_type, created_at)
        VALUES
            (#{nickname}, #{email}, #{password}, #{loginType}, NOW())

    </insert>


</mapper>